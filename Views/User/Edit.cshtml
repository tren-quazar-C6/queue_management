@model queue_management.Models.User
@{ ViewData["Title"] = "Editar Usuario"; }
<section class="module-content">
    <form class="card" asp-action="Update" method="post" style="max-width:560px;display:flex;flex-direction:column;gap:12px;">
        <h1 style="font-family:var(--font-display)">Editar usuario</h1>
        <input type="hidden" asp-for="Id" />
        <label>Documento</label>
        <input class="input" asp-for="DocumentNumber" required />
        <label>Nombre completo</label>
        <input class="input" asp-for="FullName" required />
        <button class="btn btn--primary" type="submit">Actualizar</button>
    </form>
</section>
